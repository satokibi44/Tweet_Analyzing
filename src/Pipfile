[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = "*"
pytest = "*"

[dev-packages]

[requires]
python_version = "3.8"

[scripts]
test = "python -m pytest tests"
